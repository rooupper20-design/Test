name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360 # جيتهاب يغلق السيرفر تلقائياً بعد 6 ساعات (360 دقيقة)

    steps:
      - name: Configure Core RDP Settings
        run: |
          # تفعيل سطح المكتب البعيد وإيقاف طلب NLA لسهولة الدخول من الموبايل
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force

          # فتح جدار الحماية للمنفذ 3389
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

          # إعادة تشغيل الخدمة لضمان تفعيل الإعدادات
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          # توليد كلمة سر عشوائية قوية جداً
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          
          # إنشاء المستخدم وتعيينه كأدمن
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires -Description "Kaggle RDP User"
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          
          # حفظ البيانات في متغيرات البيئة لعرضها لاحقاً
          echo "RDP_USER=RDP" >> $env:GITHUB_ENV
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV
          
          Write-Host "✅ User created successfully."

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Write-Host "📥 Downloading Tailscale..."
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          
          Write-Host "⚙️ Installing Tailscale (Fixing Quoting Error)..."
          # تم إصلاح السطر التالي ليتوافق مع صلاحيات جيتهاب
          Start-Process msiexec.exe -ArgumentList "/i `"$installerPath`" /quiet /norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          # تشغيل تيل سكيل وربطه بالتوكن الخاص بك
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          # الانتظار للحصول على الـ IP السحابي
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 12) {
              $tsIP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
              if (-not $tsIP) { Start-Sleep -Seconds 10; $retries++ }
          }
          
          if (-not $tsIP) {
              Write-Error "❌ Tailscale IP not assigned!"
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
          Write-Host "✅ Connected! IP: $tsIP"

      - name: Verify RDP Accessibility
        run: |
          # فحص داخلي للتأكد أن الويندوز جاهز لاستقبالك
          $testResult = Test-NetConnection -ComputerName localhost -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "❌ RDP port 3389 is closed inside the runner!"
              exit 1
          }
          Write-Host "✅ Server is listening on 3389!"

      - name: Maintain Connection & Show Dashboard
        run: |
          Write-Host "`n"
          Write-Host "==========================================" -ForegroundColor Cyan
          Write-Host "🚀 KAGGLE CLOUD PC IS READY!" -ForegroundColor Green
          Write-Host "==========================================" -ForegroundColor Cyan
          Write-Host "🌐 IP Address : $env:TAILSCALE_IP" -ForegroundColor Yellow
          Write-Host "👤 Username   : $env:RDP_USER" -ForegroundColor Yellow
          Write-Host "🔑 Password   : $env:RDP_PASS" -ForegroundColor Yellow
          Write-Host "==========================================" -ForegroundColor Cyan
          Write-Host "⏰ Note: Server will close in 6 hours."
          Write-Host "==========================================" -ForegroundColor Cyan
          Write-Host "`n"
          
          # حلقة البقاء حياً
          while ($true) {
              $time = Get-Date -Format "HH:mm:ss"
              Write-Host "[$time] RDP Server is Live... Connect now."
              Start-Sleep -Seconds 300
          }
